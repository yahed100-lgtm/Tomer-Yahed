# Python >= 3.10 required (PyTorch 2.x dropped support for 3.9 and below)
#
# CPU-only install:
#   pip install -r requirements.txt
#
# GPU install (CUDA 11.8):
#   pip install torch>=2.0.0 torchvision>=0.15.0 --index-url https://download.pytorch.org/whl/cu118
#   pip install numpy>=1.24.0 matplotlib>=3.7.0
#
# torch and torchvision must be installed as a matched pair.
# See https://github.com/pytorch/vision#installation for the version matrix.

torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
matplotlib>=3.7.0
